/* eslint-disable import/prefer-default-export */
export const { APP_URL } = process.env;
export const { NODE_ENV } = process.env;

// Secret keys
export const { REFRESH_TOKEN_TIME } = process.env;
export const { REFRESH_TOKEN_REMEMBER_TIME } = process.env;
export const { REFRESH_SECRET_KEY } = process.env;
export const { SECRET_KEY } = process.env;
export const { ACCESS_TOKEN_TIME } = process.env;

// Mails infos
export const { NO_REPLY_EMAIL } = process.env;
export const { MAIL_HOST } = process.env;
export const { MAIL_PORT } = process.env;
export const { MAIL_USER } = process.env;
export const { MAIL_PASS } = process.env;
